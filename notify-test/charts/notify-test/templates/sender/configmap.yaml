apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "notifyTest.sender.fullname" . }}
data:
  TARGETS: {{ .Values.sender.targets | quote }}
  TIME_PORT: {{ .Values.timePort | quote }}

  OTEL_ENABLED: "false"
  OTEL_JAVAAGENT_LOGGING: "none"
